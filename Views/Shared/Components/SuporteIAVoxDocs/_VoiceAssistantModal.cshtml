<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SuporteIA VoxDocs</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" rel="stylesheet">
    <link href="/css/SuporteModalStyles/VoiceAssistantModalStyle.css" rel="stylesheet">
</head>
<body>

    <!-- Container isolado para o Voice Assistant -->
    <div id="voxdocs-assistant-container">
        <!-- Botão flutuante -->
        <button class="floating-btn" id="floating-btn">
            <span class="material-symbols-outlined">headphones</span>
            <span class="notification-badge" id="notification-badge" style="display: none;"></span>
        </button>

        <!-- Container do Chat -->
        <div class="chat-container" id="chat-container">
            <!-- Cabeçalho -->
            <div class="chat-header">
                <div class="chat-title">
                    <span class="material-symbols-outlined">headphones</span>
                    SuporteIA VoxDocs
                </div>
                <button class="close-btn" id="close-btn">
                    <span class="material-symbols-outlined">close</span>
                </button>
            </div>

            <!-- Área de mensagens (agora vazia) -->
            <div class="messages" id="messages-container">
                <!-- As mensagens serão adicionadas dinamicamente pelo JavaScript -->
            </div>

            <!-- Área de gravação -->
            <div class="input-area">
                <div class="recording-status" id="recording-status" style="display: none;">
                    <div class="recording-dot"></div>
                    <span>Gravando</span>
                    <span class="recording-timer" id="recording-timer">0:00</span>
                </div>
                <div class="recording-progress" id="recording-progress" style="display: none;">
                    <div class="progress-fill" id="progress-fill"></div>
                </div>
                <button class="mic-btn" id="mic-btn">
                    <span class="material-symbols-outlined">mic</span>
                </button>
                <div class="text-center small text-muted mt-2 px-3 py-2 bg-primary bg-opacity-10 rounded-3 border-start border-primary border-3"
                     id="min-time-message" style="display: none;">
                    Grave pelo menos 2 segundos
                </div>
            </div>
        </div>
    </div>

    @* <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script> *@
    <script src="/js/SuporteIA/IAvoxdocs.js"></script>
</body>
</html>